{
  "name": "@dabble/rest-client",
  "version": "1.0.1",
  "description": "An easier-to-use API for using REST APIs via fetch",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "crypto-id": "^0.2.3"
  }
}
